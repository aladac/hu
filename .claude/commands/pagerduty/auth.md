Set PagerDuty API token.

## Usage

```bash
hu pagerduty auth <TOKEN>
```

## Arguments

| Arg | Description |
|-----|-------------|
| `TOKEN` | PagerDuty API token (required) |

Token stored in `~/.config/hu/credentials.toml`.
